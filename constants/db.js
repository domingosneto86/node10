module.exports = app => {

    const TABLE = Object.freeze({
        USER:     'usuario',
        CLIENT:   'cliente',
        DATA:     'cliente_data',
        PARAMETER:'parametro'     
    })

    return { TABLE }
}